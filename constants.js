const NUMERIC_TRAITS = [
  {
      "name": "Token ID",
      "ranges": [{
          "max": "10000",
          "min": "0"
      }]
  }
]

const DECIMALS = 10 ** 18

module.exports = {
    NUMERIC_TRAITS, DECIMALS
}